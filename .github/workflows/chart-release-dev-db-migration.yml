name: Chart Releaser Dev - db-migration

on:
  workflow_dispatch:
  push:
    paths:
      - 'charts/db-migration/**'

jobs:
  chart-releaser-google-registry:
    uses: ./.github/workflows/chart-releaser-google-registry.yml
    secrets: inherit
    with:
      helmChartPath: charts/db-migration